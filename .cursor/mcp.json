{
  "mcpServers": {
    "mcp-time": {
      "command": "docker",
      "args": ["compose", "-f", "mcp-time/docker-compose.yml", "up"],
      "env": {},
      "description": "日本時間のタイムスタンプを提供するMCPサーバー",
      "autoStart": true
    }
  }
} 